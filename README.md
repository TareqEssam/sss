# 🎯 المستشار الخبير - Expert Assistant

## نظام ذكي لمساعدة لجان المتابعة بالهيئة العامة للاستثمار المصرية

---

## 📋 نظرة عامة

المستشار الخبير هو نظام مساعد ذكي مصمم خصيصاً لباحثي الهيئة العامة للاستثمار المصرية، يساعد في:

✅ **التراخيص**: معرفة التراخيص اللازمة لكل نشاط والجهات المصدرة
✅ **القوانين**: الوصول للسند التشريعي وأدلة الترخيص
✅ **المناطق الصناعية**: معلومات شاملة عن المناطق وقرارات الإنشاء
✅ **القرار 104**: التحقق من أنشطة الحوافز والقطاعات
✅ **النقاط الفنية**: إرشادات دقيقة لمعاينة الشركات

---

## 🚀 المزايا الرئيسية

### 1. **ذكاء متقدم**
- فهم النية من السؤال (Intent Recognition)
- معالجة الأسئلة المعقدة والمركبة
- ذاكرة سياقية (آخر 5 محادثات)
- عتبة ديناميكية حسب نوع السؤال

### 2. **أداء عالي**
- تحميل مرة واحدة فقط (First-Time Loading)
- تخزين ذكي في IndexedDB
- بحث هجين (Vector + Keyword Matching)
- استجابة فورية بعد التحميل الأول

### 3. **دقة عالية**
- 3 قواعد بيانات متخصصة (538 متجه)
- نموذج متعدد اللغات: `paraphrase-multilingual-MiniLM-L12-v2`
- إعادة ترتيب النتائج حسب السياق
- استخراج معلومات منظمة

### 4. **واجهة احترافية**
- تصميم نظيف وسهل الاستخدام
- أسئلة سريعة جاهزة
- عرض المصادر مع درجة الثقة
- متجاوب مع الهواتف

---

## 📁 هيكل المشروع

```
expert-assistant/
├── index.html              # الواجهة الرئيسية
├── style.css              # التنسيق
├── loader.js              # محمل البيانات + IndexedDB
├── intent-engine.js       # محرك فهم النية
├── core.js               # المحرك الرئيسي
├── README.md             # هذا الملف
│
├── data/                 # قواعد البيانات (vectors)
│   ├── activity_vectors.js        # 1.07 MB - 124 متجه
│   ├── decision104_vectors.js     # 1.69 MB - 237 متجه
│   └── industrial_vectors.js      # 1.28 MB - 177 متجه
│
└── js/                   # قواعد البيانات النصية (مشوشة)
    ├── activity_database-s.js     # 397 KB
    ├── decision104_database-s.js  # 33.7 KB
    └── industrial_database-s.js   # 65.2 KB
```

---

## 🛠️ التقنيات المستخدمة

### البحث المتجه (Vector Search)
- **النموذج**: `sentence-transformers/paraphrase-multilingual-MiniLM-L12-v2`
- **الأبعاد**: 384
- **التشابه**: Cosine Similarity
- **التخزين**: IndexedDB

### معالجة اللغة العربية
- إزالة التشكيل
- توحيد الهمزات
- معالجة السياق والمرادفات

### البحث الهجين
- 70% بحث متجه (Vector Search)
- 30% مطابقة كلمات (Keyword Matching)

---

## 📖 كيفية الاستخدام

### 1. التشغيل الأول

```bash
# 1. ارفع المشروع على GitHub
git clone https://github.com/your-username/expert-assistant.git

# 2. افتح index.html في المتصفح
# أو استخدم GitHub Pages

# 3. انتظر التحميل الأول (5-10 ثواني)
# سيتم تخزين البيانات تلقائياً للمرات القادمة
```

### 2. أمثلة على الأسئلة

#### **الأنشطة**
```
ما هي التراخيص المطلوبة للنشاط الصناعي؟
من الجهة المختصة بإصدار رخصة المصنع؟
ما هو السند التشريعي للنشاط الصناعي؟
ما هي النقاط الفنية عند معاينة المصانع؟
```

#### **المناطق الصناعية**
```
ما هي المناطق الصناعية بالقاهرة؟
ما هي جهة الولاية على منطقة زهراء المعادي؟
ما هو قرار إنشاء المنطقة الصناعية؟
هل موقع X منطقة صناعية معتمدة؟
```

#### **القرار 104**
```
هل خلايا الطاقة الشمسية في القرار 104؟
ما هو قطاع خلايا الطاقة في القرار 104؟
هل النشاط الصناعي يحصل على حوافز؟
```

#### **أسئلة مركبة**
```
ما هي تراخيص النشاط الصناعي وهل هو في القرار 104؟
أين يمكن ممارسة النشاط الصناعي وما هي الجهات المختصة؟
```

### 3. الأسئلة المتتابعة
```
المستخدم: ما هي التراخيص للنشاط الصناعي؟
النظام: [إجابة عن التراخيص]

المستخدم: وما هي الجهات المختصة؟ ← يفهم "لنفس النشاط"
النظام: [إجابة عن الجهات]

المستخدم: وماذا عن النقاط الفنية؟ ← ذاكرة سياقية
النظام: [إجابة عن النقاط الفنية]
```

---

## 🔧 الدوال الرئيسية

### **loader.js**

```javascript
// التهيئة
await DataLoader.initialize()

// الحصول على البيانات
const allData = DataLoader.getAllData()
const activities = DataLoader.getDataByType('activities')

// إحصائيات
const total = DataLoader.getTotalVectors() // 538

// مسح الذاكرة (للتطوير)
await DataLoader.clearCache()
```

### **intent-engine.js**

```javascript
// تحليل السؤال
const intent = IntentEngine.parseIntent(query, history)

// استخراج الكيانات
const entities = IntentEngine.extractEntities(text)

// فهم السياق
const context = IntentEngine.buildContext(history)

// تفكيك الأسئلة المركبة
const subQueries = IntentEngine.decomposeQuery(complexQuery)
```

### **core.js**

```javascript
// الإجابة الرئيسية
const result = await ExpertAssistant.answer(query, history)

// البحث المباشر
const results = ExpertAssistant.searchVectors(query, 'activities', 5, 0.65)

// التشابه
const similarity = ExpertAssistant.cosineSimilarity(vec1, vec2)
```

---

## ⚙️ الإعدادات المتقدمة

### العتبات الديناميكية

```javascript
// في intent-engine.js
const thresholds = {
    ACTIVITY_LICENSE: 0.60,      // تراخيص الأنشطة
    INDUSTRIAL_ZONE: 0.70,       // المناطق الصناعية
    DECISION104: 0.65,           // القرار 104
    GENERAL: 0.55                // عام
}
```

### حجم الذاكرة

```javascript
// في index.html
if (conversationHistory.length > 5) {
    conversationHistory = conversationHistory.slice(-5)
}
```

### عدد النتائج

```javascript
// في core.js
let topK = 5  // افتراضي

if (intent.primary.name.startsWith('ACTIVITY')) {
    topK = 3  // أنشطة
} else if (intent.primary.name.startsWith('INDUSTRIAL_ZONE')) {
    topK = 10 // مناطق
}
```

---

## 🎨 التخصيص والتوسع

### 1. إضافة intent جديد

```javascript
// في intent-engine.js - INTENT_PATTERNS
ACTIVITY_CUSTOM: {
    keywords: ['كلمة1', 'كلمة2'],
    entities: ['كيان1'],
    threshold: 0.65
}
```

### 2. إضافة قاعدة بيانات جديدة

```javascript
// في loader.js - STORES
CUSTOM_DB: 'custom_database'

// ثم في initDB
if (!db.objectStoreNames.contains(STORES.CUSTOM_DB)) {
    db.createObjectStore(STORES.CUSTOM_DB, { keyPath: 'id' })
}
```

### 3. تعديل منطق الإجابة

```javascript
// في core.js - generateAnswer
if (intentName === 'YOUR_CUSTOM_INTENT') {
    answer = generateCustomAnswer(query, results, intent, extracted)
}
```

---

## 🐛 استكشاف الأخطاء

### المشكلة: التحميل بطيء جداً
**الحل**: تأكد من رفع ملفات vectors على CDN أو GitHub Pages

### المشكلة: النتائج غير دقيقة
**الحل**: اضبط العتبة في `getDynamicThreshold()`

### المشكلة: الذاكرة ممتلئة
**الحل**: 
```javascript
// مسح الكاش
await DataLoader.clearCache()
// ثم إعادة التحميل
location.reload()
```

### المشكلة: لا يفهم السؤال
**الحل**: أضف كلمات مفتاحية في `INTENT_PATTERNS`

---

## 📊 الأداء

| المقياس | القيمة |
|---------|--------|
| **التحميل الأول** | 5-10 ثواني |
| **التحميل من Cache** | <1 ثانية |
| **زمن الاستجابة** | 100-300ms |
| **إجمالي الـ Vectors** | 538 |
| **حجم البيانات** | ~4 MB |
| **دقة البحث** | 85-95% |

---

## 🔐 الأمان

- ✅ البيانات النصية **مشوشة** (obfuscated)
- ✅ لا استخدام لـ API خارجي
- ✅ كل العمليات **محلية** (client-side)
- ✅ IndexedDB آمن ومعزول

---

## 📝 الترخيص

هذا المشروع مخصص للاستخدام الداخلي في الهيئة العامة للاستثمار المصرية.

---

## 👨‍💻 المطور

تم التطوير بواسطة فريق التحول الرقمي - الهيئة العامة للاستثمار

---

## 🚀 التحديثات المستقبلية

- [ ] دعم البحث الصوتي
- [ ] تصدير التقارير PDF
- [ ] إحصائيات الاستخدام
- [ ] تكامل مع أنظمة الهيئة
- [ ] دعم اللغة الإنجليزية
- [ ] تطبيق موبايل

---

## 📞 الدعم الفني

للاستفسارات والدعم الفني:
- 📧 البريد: support@gafi.gov.eg
- 📱 الهاتف: 16035

---

**نسخة**: 1.0.0  
**آخر تحديث**: يناير 2026
